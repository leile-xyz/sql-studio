# 变更记录

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用语义化版本号。

## [Unreleased]

### Added

- 内置 MCP 服务：随应用启动的本地 streamable-http 服务（仅监听 `127.0.0.1:37625`），提供环境、实例、数据库、数据表与表结构的只读查询工具。
- MCP `execute_sql` 工具与本地 `POST /sql` 接口：可指定任意已配置环境执行 SQL，界面停留在其它环境时也能查询；优先复用已有会话，其次使用该环境已保存的凭据登录，不接受调用方传入账号密码。
- MCP 弹窗提供本地接口文档：`/sql` 旁的 `?` 打开参数、响应、状态码、行为边界与常见错误说明，地址与 curl 示例按当前 Token 生成，可直接复制。
- 编辑器会话过期后自动重新登录并重放一次原请求，登录弹窗与连接状态反馈同步优化。
- 资源树支持右键菜单、按需分批加载和取消搜索。

### Changed

- 控制台分页与资源搜索交互改进。
- 应用启动时展示当前版本的更新日志。

### Fixed

- 消息中心弹窗宽度适配长文本。

## [1.1.0] - 2026-07-16

### Added

- 流水线编排：SQL、消息构建和钉钉通知插件可串联执行，支持发布版本与手动触发。
- 定时计划：按天、周、月或 Cron 配置，展示下一次执行时间，支持预览与启停。
- 执行历史与执行详情、消息中心，集中查看运行结果和失败原因。
- 钉钉机器人插件：Webhook 与加签密钥保存在 Windows 凭据管理器。
- Windows 桌面端支持关闭主窗口后驻留后台，并提供系统托盘图标、窗口恢复和显式退出菜单。
- 标签栏最左侧新增固定控制台入口，支持新建、默认控制台和全部控制台列表；关闭标签不会删除控制台记录。
- 按环境持久化全部控制台、打开状态、SQL、数据库上下文、活动项和编辑器高度。
- 数据库树侧边栏支持收起和重新展开。
- 控制台下拉列表移除“已关闭”文字，并为每个控制台提供永久删除图标。
- 控制台支持通过应用内弹窗重命名，名称按环境持久化。
- 顶栏新增“关于 SQL Studio”弹窗，展示版本、客户端类型和 MIT 许可证。
- 自动联想支持字段、表、关键词和 SQL 函数的包含及跨词匹配。
- MySQL 与 PostgreSQL 方言函数候选隔离。
- 开源许可证、贡献指南、安全策略、隐私说明、社区行为准则和协作模板。

### Changed

- 完善多 SQL、PostgreSQL schema、自动联想和 Windows 桌面端文档。
- 扩展端凭据加解密失败会显式报错，不再降级为明文存储或原样返回。
- 表数据与控制台 CSV 改为按页读取全部结果后导出，表格页大小新增 1000。
- 控制台普通 SELECT 默认每页 1000 条，额外查询精确总数并支持独立翻页。

## [1.0.0] - 2026-07-10

### Added

- Chrome/Edge Manifest V3 浏览器扩展。
- Windows Tauri 2 便携桌面端。
- MySQL 与 PostgreSQL schema 资源浏览、表数据、结构查看和 SQL 查询。
- 多环境登录、查询历史、控制台草稿、CSV 导出和 Windows 凭据管理器集成。
- 多 SQL 拆分执行、结果页签、失败继续及上下文自动联想。
